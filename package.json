{
  "name": "skills2zipconvertor",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-only .skill to .zip converter.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "http-server": "^14.1.1"
  }
}
